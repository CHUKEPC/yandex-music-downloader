# YandexMusicDownloader

Удобный инструмент для скачивания музыки из Яндекс.Музыки с полными метаданными и обложками.

## ✨ Возможности

- 📀 Скачивание треков, альбомов, плейлистов и всей дискографии артистов
- 📊 Красивый progress bar с отображением прогресса скачивания
- 🎨 Автоматическое добавление обложек
- 🏷️ Полные метаданные (исполнитель, альбом, год, жанр, номер трека и т.д.)
- 📁 Умная организация файлов (отдельные папки для альбомов, синглы в одной папке)
- 🎼 Поддержка различных форматов

## 📋 Требования

- Python 3.7+
- Токен Яндекс.Музыки

## Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/CHUKEPC/yandex-music-downloader.git
cd yandex-music-downloader
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `config.py`:
```bash
cp config.example.py config.py
```

4. Отредактируйте `config.py` и добавьте ваш токен (Инструкция по получению токена: https://github.com/MarshalX/yandex-music-api/discussions/513)

### Настройка качества аудио

В `config.py` можно выбрать качество скачиваемых треков:

- **`lossless`** - Без потерь качества (FLAC, ~1000 kbps)
  - Максимальное качество звука
  - Большой размер файлов (~30-50 МБ на трек)
  - Требуется подписка Яндекс.Плюс

- **`hq`** - Высокое качество (MP3 320 kbps) ⭐ **Рекомендуется**
  - Отличное качество звука
  - Разумный размер файлов (~7-12 МБ на трек)

- **`nq`** - Нормальное качество (MP3 192 kbps)
  - Хорошее качество для повседневного прослушивания
  - Маленький размер файлов (~4-7 МБ на трек)

### Логирование

Логи пишутся только в файл (без вывода в консоль) и настраиваются в `config.py`:

- `LOGGING_ENABLED` — включение/отключение логирования (True/False)
- `LOG_FILE` — путь до файла логов (по умолчанию `logs/app.log`, директория создаётся автоматически)
- `LOG_LEVEL` — уровень (`DEBUG`/`INFO`/`WARNING`/`ERROR`/`CRITICAL`)

По умолчанию шум от внешних библиотек (например, `yandex_music`) снижен до WARNING.

### Параллельное скачивание

Чтобы ускорить загрузку альбомов и плейлистов, можно включить многопоточность:

- `MAX_CONCURRENT_DOWNLOADS` — количество одновременных загрузок (1 — без многопоточности, по умолчанию 4)

### Кэширование метаданных

Чтобы ускорить повторные загрузки, метаданные треков можно кэшировать на диске:

- `METADATA_CACHE_ENABLED` — включить/выключить кэш (True/False)
- `METADATA_CACHE_FILE` — путь к файлу кэша (по умолчанию `cache/metadata.json`, папка создаётся автоматически)
- `METADATA_CACHE_TTL_HOURS` — время жизни записей в часах (0 — без истечения)

Кэш работает прозрачно: при повторной загрузке треков метаданные берутся из файла, если запись не устарела.

## Использование

Запустите скрипт:
```bash
python main.py
```

Введите ссылку на:
- **Трек**: `https://music.yandex.ru/album/123456/track/789012`
- **Альбом**: `https://music.yandex.ru/album/123456`
- **Плейлист**: `https://music.yandex.ru/users/username/playlists/123`
- **Артиста**: `https://music.yandex.ru/artist/123456`

Во время скачивания вы увидите красивый progress bar с информацией о:
- Проценте выполнения
- Количестве скачанных треков (для альбомов/плейлистов)
- Скорости скачивания
- Оставшемся времени

## 📂 Структура проекта

```
yandex-music-downloader/
├── main.py                      # Главный файл
├── config.py                    # Конфигурация
├── requirements.txt             # Зависимости
├── utils/
│   ├── __init__.py
│   ├── file_utils.py           # Утилиты для работы с файлами
│   └── metadata.py             # Работа с метаданными
├── audio/
│   ├── __init__.py
│   └── audio_processor.py      # Обработка аудио файлов
└── downloader/
    ├── __init__.py
    ├── track_downloader.py     # Скачивание треков
    └── content_downloader.py   # Скачивание альбомов/плейлистов/артистов
```

## 📁 Организация загруженных файлов

```
music/
├── Artist Name - Track Title.mp3          # Отдельные треки
├── Album Name/                            # Альбомы
│   ├── Artist - Track 1.mp3
│   └── Artist - Track 2.mp3
├── Playlist Name/                         # Плейлисты
│   └── ...
└── artists/
    └── Artist Name/
        ├── Album Name/                    # Альбомы артиста
        │   └── ...
        └── Singles & Other Tracks/        # Синглы
            └── ...
```

## 🔧 Зависимости

- `yandex-music` - Неофициальная библиотека для работы с API Яндекс.Музыки
- `mutagen` - Работа с метаданными аудио файлов
- `requests` - HTTP запросы
- `pycryptodome` - Расшифровка зашифрованных аудио файлов (FLAC в MP4 контейнере) при скачивании в lossless качестве
- `tqdm` - Красивый progress bar для отслеживания процесса скачивания

## ⚠️ Ограничения

- Используется неофициальное API Яндекс.Музыки
- Требуется активная подписка для скачивания некоторого контента
- Некоторые плейлисты с пользовательскими загрузками могут не скачиваться

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## ⚖️ Правовая информация

Этот инструмент предназначен только для личного использования. Убедитесь, что у вас есть права на скачивание музыки. Уважайте авторские права.

## 🤝 Вклад в проект

Вклад в проект приветствуется! Если у вас есть идеи по улучшению:

1. Fork проекта
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 👤 Автор

**CHUKEPC**
- GitHub: [@CHUKEPC](https://github.com/CHUKEPC)
- Telegram: [@ArtyomRoyal](https://t.me/ArtyomRoyal)

---

<div align="center">

⭐ Если проект был полезен, поставьте звезду! ⭐

</div>